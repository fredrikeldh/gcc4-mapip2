This list is not sorted.

* Direct syscalls.
* -fomit-frame-pointer on by default at -O2.
 * If you want to debug, pass -fno-omit-frame-pointer.
* Smaller constants (8, 16, and 24-bit).
* Fix emit_move_insn bug.
* Fix builtin-bitops-1.c.
* Fix 930622-2.c.
* Fix longlong-to-float conversion.
* Direct floating-point support.
 * Let's have 16 64-bit registers.
 * Allocate f15 (the last one) for return values.
 * Save half of them. Allow parameter passing in the other half.
